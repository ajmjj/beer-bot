node_modules/
credentials.json
.env
*.json.bak

# Baileys WhatsApp session — a live credential, never commit
.baileys_auth/

# WhatsApp exports — never commit; upload via workflow_dispatch only
chat_exports/*

# Local files
.locals/*

# Local planning notes — keep out of the repo
.claude/

# WhatsApp session lock + any moved-aside session backups — runtime only, never commit
.baileys_auth.lock
.baileys_auth.*/
.baileys_auth.*/app-state-sync*
pre-key-*.json
sender-key-*.json
session-*.json
