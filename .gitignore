node_modules/
credentials.json
.env
*.json.bak

# Baileys WhatsApp session — a live credential, never commit
.baileys_auth/

# WhatsApp exports — never commit; upload via workflow_dispatch only
chat_exports/*

# Local files
.locals/*

# Local planning notes — keep out of the repo
.claude/